/* Base (mobile-first) layout */
body {
  margin: 0;
  font-family: Arial, sans-serif;
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

header,
footer {
  background-color: #9b1c58; /* Adjust colors as you like */
  color: white;
  text-align: center;
  padding: 1rem;
}

aside {
  background-color: #135589;
  color: white;
  padding: 1rem;
}

main {
  background-color: #30b27a;
  padding: 1rem;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

article {
  background-color: #d92d63;
  color: white;
  padding: 1rem;
  text-align: center;
}

footer {
  background-color: #e6732d;
  padding: 1rem;
  text-align: center;
}

/* Desktop layout (media query) */
@media (min-width: 768px) {
  body {
    display: grid;
    grid-template-areas:
      "header header"
      "aside main"
      "footer footer";
    grid-template-columns: 1fr 3fr;
    grid-template-rows: auto 1fr auto;
  }

  header {
    grid-area: header;
  }

  aside {
    grid-area: aside;
  }

  main {
    grid-area: main;
  }

  footer {
    grid-area: footer;
  }
}

